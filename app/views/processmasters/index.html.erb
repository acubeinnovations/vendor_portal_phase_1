<div class="container-fluid">
	<div class="row-fluid">
		<div class="span12">
			<a href="#" name="exportprocess" class="btn btn-primary btn-xs" style="float:right;">Export CSV</a>
				<div class="widget-box">
					<div class="widget-content nopadding">
					<table class="table table-bordered data-table">
						<thead>
						<tr>
							<th></th>
							<th>Sl</th>
							<th>Division</th>
							<th>Brand</th>
							<th>Season</th>
							<th>Market</th>
							<th>Created Date</th>
							<% allowed_users=['admin','operations']%>
							<th colspan="<% if current_user.userrole.in?(allowed_users) %>3<% else %> 1<% end %>">Actions</th>
						</tr>
						</thead>
						<tbody>
								<% if current_user.userrole=='admin' %>
									<%= render 'index_admin' %>
								<% else if current_user.userrole=='operations' %>
									<%= render 'index_op' %>
								<% else if current_user.userrole=='vendor' %>
									<%= render 'index_vendor' %>
								<% else if current_user.userrole!='vendor' && current_user.userrole!='operations' %>
									<%= render 'index_normal' %>
								<%end%>
								<%end%>
								<%end%>
								<%end%>
						</tbody>
					</table>
				</div>
		</div>
	</div>
<% if current_user.userrole.in?(allowed_users) %>	<%= link_to 'New Process', new_processmaster_path ,class:'btn btn-success' %><%end%>

</div>
<!-- EOF -->
