
<%= form_for @market, :html => {:class => "form-horizontal"} do |f| %>
	 <% if @market.errors.any? %>
		 <div id="error_explanation">
		   <h2><%= pluralize(@market.errors.count, "error") %> prohibited this post from being saved:</h2>

		   <ul>
		   <% @market.errors.full_messages.each do |message| %>
		     <li><%= message %></li>
		   <% end %>
		   </ul>
		 </div>
	 <% end %>
<div class="container-fluid">
    <div class="row-fluid">
      <div class="span12">
	<div class="widget-box">
		<div class="control-group">
		<label class="control-label">Name</label>
			<div class="controls">
					<%= f.text_field :name, class:"span6",placeholder:"Market Name" %>
			</div>
		</div>
		
	<div class="form-actions">
		<%= f.submit 'Save', :class => 'btn btn-success btn-xs' %>
	</div>  
</div>

</div>
</div>


<% end %>
