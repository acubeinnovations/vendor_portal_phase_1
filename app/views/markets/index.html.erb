<div class="container-fluid">
    <div class="row-fluid">
      <div class="span12">
        <div class="widget-box">
          <div class="widget-content nopadding">
            <table class="table table-bordered data-table">
              <thead>
                <tr>
                  <th>Sl</th>
                  <th>Market Name</th>
                  <th>Created Date</th>
                  <th colspan='2'>Actions</th>
                </tr>
              </thead>
              <tbody>
								<% @i=1 %>
								<% @markets.each do |market| %>
									<tr class="gradeX">
										<td><%= @i %></td>
										<td><%= market.name %></td>
										<td><%= market.created_at %></td>
										<td><%= link_to 'Edit', edit_market_path(market),class:'btn btn-info' %></td>
										<td><%= link_to 'Delete', market, method: :delete, data: { confirm: 'Are you sure?' },class:'btn btn-danger' %></td>
									</tr>
									<% @i+=1%>
								<% end %>	
							</tbody>
            </table>
				
         	</div>
       </div>
   </div>
			<%= link_to 'New Mmarket', new_market_path ,class:'btn btn-success' %>
</div>

